<?php 

$sql = "SELECT * FROM membre ORDER BY nom, prenom";
$membres = executeRequete($sql);

$table = '';
while ($data = $membres->fetch_assoc()) {

  $table .= "<tr><td>". $data['id_membre'] . "</td><td>". $data['nom'] . "</td><td>". $data['prenom'] . "</td><td>". $data['email'] . "</td><td>". $_trad[$data['statut']] . "</td>";
  $table .= "<td><a href='". LINK.LINKADM.'?nav=profil&id='.$data['id_membre'] . "'>modifier</td>";
  $table .= "</tr>";
  # code...
}

$table = "<table>$table</table>";

?>



    <div class="container">

      <div class="starter-template">
        <h1><span class="glyphicon glyphicon-pencil "></span><?php echo $titre; ?></h1>
		<hr />
      </div> 
      <div class="">
        <?php 
        echo $table; 
        ?>
		<hr />
      </div>
    </div><!-- /.container -->
